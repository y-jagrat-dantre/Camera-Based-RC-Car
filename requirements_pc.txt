# requirements_pc.txt
# ====================
# Python dependencies for running on a PC (no Raspberry Pi hardware needed).
# Excludes pigpio (Pi-only) and picamera2 (Pi-only).

opencv-python>=4.9.0           # Use full version on PC (includes GUI)
ultralytics>=8.2.0             # YOLOv8
flask>=3.0.0
flask-socketio>=5.3.0
pyyaml>=6.0.1
numpy>=1.26.0
pynput>=1.7.6                  # Keyboard input for simulated receiver
