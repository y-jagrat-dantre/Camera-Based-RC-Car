opencv-python-headless>=4.9.0
ultralytics>=8.2.0
flask>=3.0.0
flask-socketio>=5.3.0
pyyaml>=6.0.1
numpy>=1.26.0
# pigpio is Pi-only — installed separately via apt
# picamera2 is Pi-only — installed separately via apt
